// Legacy plugin intentionally kept as no-op.
// Apollo error-link ownership moved into `plugins/30.apollo.client.ts`
// to guarantee deterministic single-owner lifecycle.
export default defineNuxtPlugin(() => {});
